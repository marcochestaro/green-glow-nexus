name: Sync Dashboard to gh-pages

on:
  push:
    branches: [main]
    paths:
      - 'dashboard/**'

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Sync dashboard to gh-pages
        run: |
          cp dashboard/index.html /tmp/index.html
          cp dashboard/scripts-data.json /tmp/scripts-data.json 2>/dev/null || true
          cp dashboard/agents-data.json /tmp/agents-data.json 2>/dev/null || true
          cp dashboard/miro-boards.json /tmp/miro-boards.json 2>/dev/null || true
          cp dashboard/data.json /tmp/data.json 2>/dev/null || true
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git fetch origin gh-pages
          git checkout gh-pages
          cp /tmp/index.html index.html
          [ -f /tmp/scripts-data.json ] && cp /tmp/scripts-data.json scripts-data.json || true
          [ -f /tmp/agents-data.json ] && cp /tmp/agents-data.json agents-data.json || true
          [ -f /tmp/miro-boards.json ] && cp /tmp/miro-boards.json miro-boards.json || true
          [ -f /tmp/data.json ] && cp /tmp/data.json data.json || true
          git add index.html scripts-data.json agents-data.json miro-boards.json data.json 2>/dev/null || git add index.html
          git diff --staged --quiet || git commit -m "Auto-sync dashboard to gh-pages $(date -u '+%Y-%m-%d %H:%M UTC')"
          git push --force origin gh-pages
